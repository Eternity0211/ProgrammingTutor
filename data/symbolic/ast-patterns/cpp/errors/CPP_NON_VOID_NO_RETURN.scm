; ============================================================================
; CPP_NON_VOID_NO_RETURN
; 规则：非 void 返回类型的函数如果在整个函数体中没有任何 return
;       语句，则可能导致未定义行为或逻辑错误。
;
; 实现方式：通过正则匹配检查 function_definition 节点源码中是否既不
; 包含 "void" 关键字（排除 void 函数），又不包含任何 "return"。
; 这是一个启发式检测，能够覆盖绝大多数简单场景。
;
; 约定：
; - @target：匹配到的 function_definition 节点
; ============================================================================

(function_definition) @target
(#not-match? @target "\\bvoid\\b[^\\(]*\\(")
