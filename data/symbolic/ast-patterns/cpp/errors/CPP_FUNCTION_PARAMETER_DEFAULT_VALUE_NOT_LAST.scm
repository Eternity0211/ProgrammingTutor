; ============================================================================
; CPP_FUNCTION_PARAMETER_DEFAULT_VALUE_NOT_LAST
; 规则：当函数参数列表中某个参数拥有默认值，但其后仍出现不带默认值的
;       参数时，C++ 语法将拒绝编译。此规则用于尽早提醒开发者调整顺序。
;
; 实现方式：对 parameter_list 节点的源码进行正则检查，识别“在等号
; (=)后面出现逗号且逗号后第一个非空白字符不是等号”的模式。
; 这是一个简单启发式规则，适用于常见情境。
;
; 约定：
; - @target：匹配到的 parameter_list 节点
; ============================================================================

(parameter_list) @target
