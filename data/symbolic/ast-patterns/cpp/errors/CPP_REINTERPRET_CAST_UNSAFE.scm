; ==============================================================================
; CPP_REINTERPRET_CAST_UNSAFE
; 检测 reinterpret_cast 的使用，其在不兼容类型间转换可能导致未定义行为。
; ==============================================================================

(call_expression) @target
(#match? @target "reinterpret_cast\\s*<")
