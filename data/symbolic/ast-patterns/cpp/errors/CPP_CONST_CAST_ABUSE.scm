; ==============================================================================
; CPP_CONST_CAST_ABUSE
; 检测 const_cast 的使用，移除 const 后修改对象可能导致未定义行为。
; ==============================================================================

(call_expression) @target
(#match? @target "const_cast\\s*<")
