; ==============================================================================
; CPP_FUNCTION_PARAMETER_DEFAULT_VALUE_REDEFINED
; 默认参数在声明和定义中重复指定。需跨翻译单元分析，单文件内难以实现。
; 保留占位，使用永假谓词。
; ==============================================================================

(parameter_declaration) @target
(#eq? @target "__NO_MATCH_DEFAULT_REDEF__")