; ==============================================================================
; CPP_MACRO_REDEFINITION
; 同一宏被多次定义。需跨文件/预处理单元分析，单文件内可检测重复 #define。
; ==============================================================================

; 单文件内检测同一宏名多次出现（需 validator 比较）
(preproc_def
  name: (identifier) @target
)